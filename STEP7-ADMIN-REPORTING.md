# Step 7 — Admin, Reporting & Operations

Admin now has API-backed performance reporting, per-office metrics, daily volume, priority counts, transfer audit history, user activation/deactivation, account creation, and service-window controls.

Before production, run `CampusQ_DatabaseSetup.sql` so the `Users.IsActive` column exists.
